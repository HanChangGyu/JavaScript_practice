// 함수 선언

function getAree (width, height) { // 전달된 인수를 순서대로 저장하는 매개변수
    let area = width * height;

    console.log(area);
}

getAree(10, 20); // 함수에게 전달한 값 인수
getAree(30, 20);

// 호이스팅
// -> 끌어올리다 라는 뜻